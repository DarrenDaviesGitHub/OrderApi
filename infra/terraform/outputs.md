Outputs from the Terraform deployment:

- `apim_management_url` — the gateway URL for the APIM instance

- `apim_publisher_email` — publisher email set on the APIM

Use `terraform output` to see values after apply.
